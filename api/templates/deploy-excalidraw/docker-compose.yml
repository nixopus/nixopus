services:
  excalidraw:
    image: ${IMAGE:-excalidraw/excalidraw}:${TAG:-latest}
    container_name: ${CONTAINER_NAME:-excalidraw}
    ports:
      - "${HOST_PORT:-5000}:${CONTAINER_PORT:-80}"
    restart: unless-stopped
